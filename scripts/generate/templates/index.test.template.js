const {
  expect,
  describe,
  it,
  beforeAll,
  beforeEach,
  afterEach,
} = require("@jest/globals");

describe("", () => {
  beforeAll(() => {});
  beforeEach(() => {});
  afterEach(() => {});
  it("", () => {
    expect(true);
  });
});
